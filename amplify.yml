version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - yarn site
      artifacts:
        baseDirectory: docs
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: packages/plugin-packer
