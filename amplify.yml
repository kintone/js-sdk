version: 1
applications:
  - appRoot: packages/plugin-packer
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../../
            - yarn install
            - cd packages/plugin-manifest-validator
            - yarn build
            - cd ../plugin-packer
        build:
          commands:
            - yarn site
      artifacts:
        baseDirectory: docs
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
